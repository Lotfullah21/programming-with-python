# Multiplication Operator

The `*` operator multiplies two numbers.

```python
a = 12
b = 10
product = a * b

print(product)  # 120
```

`*` can also repeat a string or list:

```python
print("ha" * 3)  # hahaha
```
